/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  // Descomentar y modificar si tu repo no es el principal:
basePath: '/Trabajo-full-stack',
}

module.exports = nextConfig
